import 'package:Joswor/core/contstant/imageAssets.dart';
import 'package:flutter/material.dart';
import 'package:flutter_svg/flutter_svg.dart';

class SvgsWidget {
  Widget menue = SvgPicture.asset(
    AppImageAsset.menue,
    color: Colors.white,
  );
  Widget articles = SvgPicture.asset(
    AppImageAsset.article,
    color: Colors.white,
  );
  Widget offers = SvgPicture.asset(
    AppImageAsset.offers,
    color: Colors.white,
  );

  Widget home = SvgPicture.asset(
    AppImageAsset.home,
    color: Colors.white,
  );
  Widget university = SvgPicture.asset(
    AppImageAsset.university,
    color: Colors.white,
  );
  Widget submission = SvgPicture.asset(
    AppImageAsset.submission,
    color: Colors.white,
  );
}

class Svgs {
  SvgPicture home = SvgPicture.asset(
    AppImageAsset.home,
    color: Colors.white,
  );
  SvgPicture articles = SvgPicture.asset(
    AppImageAsset.article,
    color: Colors.white,
  );
  SvgPicture offers = SvgPicture.asset(
    AppImageAsset.offers,
    color: Colors.white,
  );
  SvgPicture university = SvgPicture.asset(
    AppImageAsset.university,
    color: Colors.white,
  );
  SvgPicture submission = SvgPicture.asset(
    AppImageAsset.submission,
    color: Colors.white,
  );
}
